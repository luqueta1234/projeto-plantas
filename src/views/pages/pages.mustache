{{>partials/header}}

{{#banner}}
<section class="banner" style="background-image: url('images/{{background}}'0)">{{title}}</section>
<h2>{{title}}</h2>
{{/banner}}

<div class="container list">
    {{#list}}

    <div class="item">
        <img src="images/{{image}}" class="item--image" />
        <div class="item--name">{{name}}</div>
    </div>

    {{/list}}
</div>

{{^list}}
    
    <div class="container no-list">
        <h2>Não encontramos plantas disponíveis com esse nome.</h2>
    </div>

{{/list}}

{{>partials/footer}}